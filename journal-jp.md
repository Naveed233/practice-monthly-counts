
# 作業ログまとめ

詳細な時系列ログ: [journal.md](./journal.md)（英語・作業当時のまま）

## 進め方

環境構築 → シードデータ投入 → API（固定値 → クエリパラメータ → Supabase 接続 →
ページング → 月別集計）→ UI（useState / useEffect / テーブル表示）→
統合テスト → Vercel デプロイ → SQL 突き合わせ検証。
各ステップで動作確認をしてから次へ進む方式で実施。

## つまずきと解決

| 事象 | 原因 | 解決 | 学び |
|---|---|---|---|
| シード SQL が実行できない | LLM 生成 SQL に誤り3件（`union all_rows` 構文、列名の綴り、テーブル名） | 3件とも自分で特定して修正 | LLM の出力は必ず自分で検証してから使う |
| API が 0 行を返す（エラーなし） | RLS 有効・ポリシー未設定。SQL エディタは owner 権限のため気づけない | anon ロール向け SELECT ポリシーを追加 | 「0行・エラーなし・anon キー」→ まず RLS を疑う |
| project_1 が 1,000 行しか返らない | PostgREST の既定取得上限による無音の切り捨て | `.range()` の 1,000 行バッチでページング実装 | 上限はエラーを出さない。全件必要なら必ずページング |
| ホームページが表示されない（API は正常） | `app/page.tsx` と `pages/index.tsx` が `/` を取り合い | 不要な `app/` を削除（仕様は Pages Router） | 「API は動くのに画面が出ない」→ 残存 `app/` を疑う |
| TS: Property 'month' does not exist on type 'never' | `useState([])` は型情報ゼロ → `never[]` と推論 | `useState<{month: string; count: number}[]>([])` で行の形を宣言 | 空配列で初期化する state には型注釈を付ける |
| 実行時: Objects are not valid as a React child | セルに配列全体（`rows`）を描画していた | `row.month` / `row.count` に修正 | ループ・map の中では必ずループ変数を使う（外側のコレクションは使わない） |
| SQL で 0 行（エラーなし） | `'project-3'`（ハイフン）と `'project_3'`（アンダースコア）の不一致 | 値を修正 | 空結果 ≠ 壊れたクエリ。まずフィルタ値の綴りを確認 |
| テストがタイムアウト | 初回コンパイル + 3 回の逐次フェッチ > 既定 5 秒 | タイムアウトを 15 秒に設定（開発サーバー起動も前提） | 統合テストは実行環境の前提（サーバー起動・所要時間）を明示する |

## ルール化した学び

1. 主張・実装の前に、その環境の設定とコードで裏を取る（既定値を記憶で断定しない）
2. 無音の失敗パターンに注意: 取得上限の切り捨て / RLS の空結果 / フィルタ値の不一致
   — いずれもエラーを出さずに「それらしい」結果を返す
3. ループ・map の中ではループ変数を使う（tally ループ・option 値・テーブルセルで計3回発生）
4. サンプルコードは貼り付けではなく、名前を1つずつ自分のコードに置き換える
5. レスポンスは1リクエストにつき1回のみ（ループ内で `res.json()` しない）

## 検証

- API 出力と Supabase の SQL GROUP BY を月ごとに突き合わせ、3プロジェクトすべて一致
  （project_1: 394/391/389/389/389/388、合計 2,340）
- テスト: 成功 → ページングを外すと失敗（合計 1,000）→ 復元して成功、を確認
- デプロイ後、本番 URL でも3プロジェクトの値が一致することを確認
